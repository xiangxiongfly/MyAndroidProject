<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MyStatusBar" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="textAllCaps">false</item>
    </style>

    <style name="FullScreenTheme" parent="Theme.MyStatusBar">
        <!--   是否显示ActionBar     -->
        <item name="windowActionBar">false</item>
        <!--   是否显示标题栏     -->
        <item name="windowNoTitle">true</item>
        <!--   是否全屏显示，会隐藏状态栏和导航栏    -->
        <item name="android:windowFullscreen">true</item>
        <!--   无窗口内容覆盖   -->
        <item name="android:windowContentOverlay">@null</item>
        <!--   设置状态栏颜色     -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <!--   导航栏是否是半透明     -->
        <item name="android:windowTranslucentNavigation">true</item>
    </style>

    <style name="FullScreenTheme2" parent="Theme.MyStatusBar">
        <!--   是否显示ActionBar     -->
        <item name="windowActionBar">false</item>
        <!--   是否显示标题栏     -->
        <item name="windowNoTitle">true</item>
        <!--   设置状态栏颜色     -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <!--   导航栏是否是半透明     -->
        <item name="android:windowTranslucentNavigation">true</item>
    </style>

    <style name="NoActionBarTheme" parent="Theme.MyStatusBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="TransparentTheme" parent="Theme.MyStatusBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

</resources>